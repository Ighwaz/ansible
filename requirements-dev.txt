# Werkzeuge für die Qualitätsprüfung (lokal und in der CI).
#
#   pip install -r requirements-dev.txt
#
# Bewusst auf die Patch-Ebene gepinnt (~=): Fehlerkorrekturen kommen
# automatisch, neue Minor-Versionen nicht. ansible-lint bringt mit neuen
# Minor-Versionen regelmäßig zusätzliche Regeln mit — die sollen die CI
# nicht unvermittelt rot färben, sondern beim bewussten Anheben auffallen.
ansible-lint~=26.8.0
yamllint~=1.38.0
